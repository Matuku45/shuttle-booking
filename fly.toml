app = "shuttle-booking-app-rough-wind-1710"
primary_region = "jnb"

[build]

[http_service]
  internal_port = 80
  force_https = true
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

[env]
  NODE_ENV = "production"

[deploy]
  # optional: specify Dockerfile if using custom build
